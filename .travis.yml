language: go
os: linux
env:
  - GO111MODULE=on

jobs:
  include:
    - stage: Build
      name: Server
      before_install:
        - cd server
      install: skip
      script: 
        - go build
    - stage: Test
      name: Server
      before_install:
        - cd server
      install: skip
      script:
        - go test ./... -v -cover
